apiVersion: apps/v1
kind: DaemonSet
metadata:
  namespace: default
  name: my-ds
  labels:
    app: my-ds-app
spec:
  template:
    metadata:
      labels:
        app: my-ds-app
    spec:
      containers:
        - name: my-container
          image: mysql:latest
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "redhat"
  selector:
    matchLabels:
      app: my-ds-app
  